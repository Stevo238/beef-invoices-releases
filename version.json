{
  "version": "1.1.1",
  "url": "https://github.com/Stevo238/beef-invoices-releases/releases/download/v1.1.1/BeefInvoices.exe",
  "notes": "Fixes the Wix payment bridge: Wix requires an id on every payment, so addPayment was rejected by the type checker. The app now sends a stable id per allocation, so retrying a call that timed out re-uses it instead of recording a second payment. Requires republishing the Wix site with the updated http-functions.js."
}

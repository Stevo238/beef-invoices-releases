{
  "version": "1.4.1",
  "url": "https://github.com/Stevo238/beef-invoices-releases/releases/download/v1.4.1/BeefInvoices.exe",
  "notes": "Fixes the dead UI - no tabs, no history, no money. app.js had a real newline inside a string literal, which is a JavaScript syntax error, so the browser refused to run any of the file. Every server-side test passed throughout because the file serves fine; only a browser could tell. The build now parses app.js and refuses to ship if it is invalid."
}
